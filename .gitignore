# env
.env
.env.*
!.env.example
!.env.prod.example

# scratches for agents or smth (symlink to ../_shared/molvia/scratch)
.scratch/
.scratch

# lavish review artifacts (symlink to ../_shared/molvia/lavish)
.lavish/
.lavish

# deps and build output
node_modules/
dist/
coverage/
certs/
*.pem
dev-dist/
*.tsbuildinfo

# playwright artifacts
playwright-report/
test-results/
blob-report/
.playwright/

# os / editor
.DS_Store

# adversarial attack files: copied in to run, they belong to .scratch. Both halves, because
# `git add -A` would otherwise carry the frontend's into a commit (MOL-25, round 3).
backend/tests/*.attack.*
frontend/src/*.attack.*
