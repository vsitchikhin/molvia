-- Удалённый обмен остаётся строкой до следующего запроса владельца: «Вернуть» снимает пометку, и
-- обмен сохраняет свой created_at — по нему упорядочен день и считается подсказка остатка (MOL-40,
-- адверсариальный раунд 2, В1, В2). Повтор переживает.
ALTER TABLE "exchanges" ADD COLUMN IF NOT EXISTS "deleted_at" timestamp with time zone;
