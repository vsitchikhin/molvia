-- Личный синоним (MOL-45, В-3): запрос ничего не нашёл, и позицию взяли по другому запросу. Такая
-- строка впускает позицию в ответ ровно на этот запрос и только этому человеку. Все прежние
-- выборы — обычные, ничего не впускают. Без потери данных. Повтор переживает.
ALTER TABLE "search_picks" ADD COLUMN IF NOT EXISTS "admits" boolean DEFAULT false NOT NULL;
