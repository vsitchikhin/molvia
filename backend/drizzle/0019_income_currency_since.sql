-- Когда в последний раз сменилась валюта пересчёта (MOL-42, В-2): смена действует вперёд, и свой
-- курс в новой валюте строится только из обменов с этого дня. Пусто — валюту не меняли, отсекать
-- нечего. Повтор переживает.
ALTER TABLE "actors" ADD COLUMN IF NOT EXISTS "income_currency_since" timestamp with time zone;
